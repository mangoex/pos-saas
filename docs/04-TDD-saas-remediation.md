# TDD SaaS vigente — cierre A01–A12

Estado inicial: diseño, sin certificación. Identificar primero prueba focal fallida por razón esperada; no desactivar tests heredados para obtener verde.

## TDD-TS-200 PRD-FR-001: Sign Up Público Autoservicio

Verificar BDD-SC-700 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-201 PRD-FR-002: Aprovisionamiento Atómico de Tenant

Verificar BDD-SC-701 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-202 PRD-FR-003: Aislamiento Multi-Tenant Estricto

Verificar BDD-SC-702 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-203 PRD-FR-004: Wizard de Onboarding en 3 Pasos (5 Minutos)

Verificar BDD-SC-703 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-204 PRD-FR-005: Gestión de Planes y Suscripción

Verificar BDD-SC-704 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-205 PRD-FR-010: Catálogo Simplificado de Productos

Verificar BDD-SC-705 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-206 PRD-FR-011: Precios Diferenciados por Canal (Salón vs Delivery)

Verificar BDD-SC-706 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-207 PRD-FR-012: Variantes y Modificadores

Verificar BDD-SC-707 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-208 PRD-FR-013: Kill-Switch y Agotados por Canal

Verificar BDD-SC-708 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-209 PRD-FR-020: Interfaz Táctil Optimizada

Verificar BDD-SC-709 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-210 PRD-FR-021: Captura Dinámica de Comandas

Verificar BDD-SC-710 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-211 PRD-FR-022: Modalidades de Venta

Verificar BDD-SC-711 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-212 PRD-FR-023: División de Cuentas (Split Bill)

Verificar BDD-SC-712 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-213 PRD-FR-024: Propinas Integradas

Verificar BDD-SC-713 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-214 PRD-FR-025: Cobro Multiforma de Pago

Verificar BDD-SC-714 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-215 PRD-FR-026: Inmutabilidad y Auditoría de Pagos

Verificar BDD-SC-715 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-216 PRD-FR-027: Impresión Térmica Desatendida

Verificar BDD-SC-716 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-217 PRD-FR-028: Resiliencia y Modo Offline

Verificar BDD-SC-717 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-218 PRD-FR-030: Apertura de Turno con Fondo Inicial

Verificar BDD-SC-718 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-219 PRD-FR-031: Movimientos de Turno (Entradas y Retiros)

Verificar BDD-SC-719 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-220 PRD-FR-032: Corte X (Corte Parcial Informativo)

Verificar BDD-SC-720 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-221 PRD-FR-033: Corte Z (Cierre Ciego y Cuadre de Turno)

Verificar BDD-SC-721 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-222 PRD-FR-034: Notificación Inmediata al Propietario

Verificar BDD-SC-722 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-223 PRD-FR-040: Bandeja Centralizada de Delivery

Verificar BDD-SC-723 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-224 PRD-FR-041: Inyección Automática a Cocina

Verificar BDD-SC-724 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-225 PRD-FR-042: Idempotencia de Webhooks de Plataformas

Verificar BDD-SC-725 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-226 PRD-FR-043: Kill-Switch Global de Platillos (1 Clic)

Verificar BDD-SC-726 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-227 PRD-FR-050: Token Único y Código QR en Ticket

Verificar BDD-SC-727 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-228 PRD-FR-051: Portal Comensal Móvil de Autofacturación

Verificar BDD-SC-728 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-229 PRD-FR-052: Timbrado CFDI 4.0 Automatizado (FacturAPI / PAC)

Verificar BDD-SC-729 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. El transporte falso debe comprobar auditoria de emision, cancelacion y recibo para confirmacion, simulacion y error: solo metadatos minimos, actor real/efectivo y correlacion; timeout/error conserva confirmacion `unknown` y ningun efecto fiscal local. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-230 PRD-FR-053: Descarga y Envío Automático

Verificar BDD-SC-730 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-231 PRD-FR-054: Factura Global Automatizada

Verificar BDD-SC-731 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-232 PRD-FR-060: Menú Digital Responsivo (PWA)

Verificar BDD-SC-732 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-233 PRD-FR-061: Personalización de Platillos en Menú Web

Verificar BDD-SC-733 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-234 PRD-FR-062: Carrito y Checkout para WhatsApp

Verificar BDD-SC-734 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-235 PRD-FR-063: Generación de Pedido Formateado a WhatsApp

Verificar BDD-SC-735 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-236 PRD-FR-070: Dashboard Ejecutivo Resumido

Verificar BDD-SC-736 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

BDD-SC-813: `test_executive_ai.py` captura el contexto del proveedor simulado y exige snapshots
confirmados de la sucursal autorizada y `NOT_AVAILABLE` cuando no existe costo trazable.

## TDD-TS-237 PRD-FR-071: Gestión de Personal con PIN de 4 Dígitos

Verificar BDD-SC-737 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-238 PRD-FR-072: Reporte Automatizado al Dueño

Verificar BDD-SC-738 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-239 PRD-NFR-001: El POS debe responder a interacciones táctiles en < 100 ms.

Verificar BDD-SC-739 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-240 PRD-NFR-002: Garantía absoluta de aislamiento de datos a nivel de base de datos (`organization_id` en todas las capas).

Verificar BDD-SC-740 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-241 PRD-NFR-003: Uptime del 99.9% para la API central y el portal de autofacturación.

Verificar BDD-SC-741 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-242 PRD-NFR-004: El POS debe tolerar hasta 2 horas de trabajo sin internet localmente.

Verificar BDD-SC-742 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-243 PRD-NFR-005: Generación y respuesta de timbrado CFDI 4.0 en < 5 segundos.

Verificar BDD-SC-743 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-244 PRD-NFR-006: Interfaces diseñadas para tablets de bajo costo y celulares bajo estándar WCAG 2.1 AA.

Verificar BDD-SC-744 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-245 PRD-FR-080: Autoridad superadmin persistida, aprovisionamiento administrativo auditable y soporte con actor real, actor efectivo y tenant destino; jamás autoridad por correo ni bootstrap desde login público.

Verificar BDD-SC-745 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-246 PRD-FR-081: Identidad pública exacta del restaurante, estado local y PWA aislados; slug inválido o ambiguo falla explícitamente sin catálogo sustituto.

Verificar BDD-SC-746 con fixture de dos organizaciones. El alta superadmin y `setup-my-restaurant` deben devolver `public_slug` y `menu_url`; resolver cada slug debe devolver sólo su sucursal y una clave distinta. Repetir setup conserva slug y clave, deja `onboarding_step=complete` y trial de catorce días. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Evidencia local: `test_superadmin_create_tenants_with_different_menu_modes` y `test_superadmin_list_and_create_administrators` (SQLite); PostgreSQL/concurrencia siguen pendientes.

## TDD-TS-247 PRD-FR-082: Importaciones y toda administración validan actor, organización y sucursal de destino antes de efectos.

Verificar BDD-SC-747 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-248 PRD-FR-083: Primera venta ligera sin recetas, compras ni costeo de insumos; módulos ERP excluidos del producto, preservando historia y compensaciones.

Verificar BDD-SC-748 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-249 PRD-NFR-007: Release exige evidencia diferenciada local/PostgreSQL/CI/Git/despliegue/canary; verificar aislamiento de infraestructura Kiwi y SaaS, backups restaurables y reversión antes de habilitación comercial.

Verificar BDD-SC-749 con fixture de dos organizaciones. Oráculo: resultado funcional completo del requisito, efectos persistidos en A y ausencia de efectos/lecturas en B; no basta HTTP 200 ni texto de UI. Cubrir rechazo de entradas inválidas y rollback si hay escritura. Evidencia ejecutable y resultado pendientes; no atribuir suites ERP por coincidencia de IDs.

## TDD-TS-300 Restaurante desconocido y cambio de negocio

BDD-SC-800, BDD-SC-812. A01/A10: resolver exacto, aliases ambiguos 409, claves no creadas por GET, navegación tacos→sushi→tacos, favoritos/carrito y manifest/scope independientes; QA móvil y escritorio. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

## TDD-TS-301 Mutaciones cruzadas rechazadas

BDD-SC-801. A02/A06: matriz A→A, A→B, B→A para usuarios, suspensión, roles, importaciones, catálogo, órdenes, caja, KDS, impresión y reportes; repetir PostgreSQL. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

Caja C1/C2: parametrizar SQLite/PostgreSQL con A/B; conceptos homónimos y la misma key no
colisionan, replay propio permanece estable, B no versiona/archiva A y concepto B se rechaza
antes de movimiento/comando A. Verificar organización persistida en movimiento, comando y
ledger. C3 de cortes de usuario conserva un paquete separado.

C3: A/B niega detalle y decisión de reapertura ajena antes de mutar; el replay y command
lock se separan por organización y la solicitud propia conserva su estado.

Reportes: A/B inserta un retiro confirmado por tenant y exige que `expenses` devuelva sólo el
movimiento del actor/sucursal autorizada; repetir SQLite/PostgreSQL. Las proyecciones de ventas,
correcciones e insumos usan la misma organización efectiva.

Delivery y personal: `test_uber_status_update_rejects_order_from_another_tenant` intenta cambiar
por UUID un pedido B con token A y exige 403/404 sin modificar pedido ni metadatos; los ciclos
propios de Uber, DiDi y Rappi deben permanecer verdes.
`test_drivers_and_attendance_are_isolated_by_actor_tenant` usa el mismo código de empleado en A/B,
exige listados propios, rechazo de edición cruzada y asistencia ligada al repartidor del tenant.

## TDD-TS-302 Correo no concede autoridad

BDD-SC-802, BDD-SC-810, BDD-SC-811. A03/A12: flag persistido y revocado, ausencia de bootstrap HTTP, superadmin suspendido sin acceso ni auto-reactivación, provisionador rechaza cuentas tenant existentes e impersonación registra emisión con actor real, efectivo y tenant. La regresión `test_saas_support_context.py` exige atribución y correlación de intentos/eventos de dominio, y revocación inmediata del emisor del token de soporte; sin defaults ni secretos. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

## TDD-TS-303 Signup atómico con permisos sembrados

BDD-SC-803. A04: test_saas_onboarding.py, duplicados, rollback y concurrencia PostgreSQL; no eliminar constraint. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

## TDD-TS-304 Límite exacto de prueba y suspensión

BDD-SC-804. A05: reloj inyectable, instante anterior/al borde/posterior, UTC y zonas locales, plan inválido, activación sin cobro no permitida; no sleeps. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

## TDD-TS-305 Webhook sin autenticidad no produce pedidos

BDD-SC-805. A07: probar cada proveedor, tenant por tienda, secreto faltante, payload original, inbox, carreras PostgreSQL y sandbox autorizado. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

Para DiDi Food y Rappi, la regresión de configuración prueba aislamiento por organización,
secretos redactados al leer y persistencia deshabilitada como `PENDING_VALIDATION`. La prueba
semántica del Hub exige que guardar no muestre "Conectado" ni habilite tráfico. No existe una
prueba de contrato de partner hasta que se autorice esa integración.

## TDD-TS-306 Estado remoto exige confirmación

BDD-SC-806. A08: adaptador real, timeout, rechazo, retry y reconciliación; la prueba con mock no sustituye canary proveedor. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

## TDD-TS-307 Captura canónica hasta cocina

BDD-SC-807. A09: test_saas_whatsapp_menu.py y contrato real web→POS→KDS; clave repetida mismo payload, conflicto distinto payload, atomicidad y rate limit. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

## TDD-TS-308 Primera venta sin ERP

BDD-SC-808. A11: `tests/frontend/test_saas_lean_scope.mjs` verifica que Admin no enlace ni monte recetas multinivel, producción, almacenes o traspasos; las URLs históricas presentan estado fuera de alcance y las compras nuevas son sólo caja. Conserva accesibles mermas, caja y extras. Typecheck/build Admin requeridos; histórico financiero y PostgreSQL siguen pendientes. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.

`test_parse_supplier_invoice_data_never_invents_missing_values` exige que texto ilegible o líneas
sin cantidad verificable devuelvan cero líneas, total cero y proveedor/folio nulos. La suite
`test_inventory_ai.py` conserva el parseo explícito y verifica autorización del endpoint.

## TDD-TS-309 Liberación con evidencia verificable

BDD-SC-809. A12: traceability, CI aplicable, PostgreSQL forward/rollback, restore, recursos, dominio→imagen→base y canary compensable. Estado: pruebas requeridas, ejecución pendiente. Registrar comando, SHA, entorno, resultado y riesgo residual antes de promover estado.


## Gates comunes del paquete

Focales Python, ruff/mypy backend, typecheck y semántica frontend, build y QA visual afectados; suite completa en CI una vez según AGENTS. PostgreSQL cubre unicidad, locks, idempotencia y migraciones; SQLite/gateway cubren replay/autorización si afectados. Registrar afirmación R3, evidencia, intento de refutación, resultado y riesgo residual en cierre. Auditoría Sol independiente con contexto fresco obligatoria. No declarar Probado por documentación ni por tests de servicio sin recorrido integral.


La evidencia transversal de migraciones, signup y conservación tras reversión se ejecuta
con `SAAS_TEST_POSTGRES_URL` y `apps/api/tests/test_saas_postgres_release.py`: esquema
aleatorio exclusivo, cadena Alembic real y limpieza limitada a ese esquema. La ausencia
local de variable produce skip explícito; CI la suministra y debe ejecutar el caso.
La primera caja/pedido/cobro/cierre/replay se verifica además en `test_saas_cash_scope.py`.

El contrato frontend `test_pos_session_handoff.mjs` verifica que únicamente `import.meta.env.DEV` active los servidores separados; el bundle compilado mantiene el origen al recuperar o transferir la sesión.

La revocación del emisor de soporte se verifica también con esquemas HTTP Bearer, bearer y bEaReR; todos deben rechazar sin modificar el negocio. El parser de onboarding y atribución de soporte es compartido.

La regresión de clientes incluye editar direcciones de A desde B y marcar una como predeterminada: debe rechazar sin modificar ninguna dirección de A; la edición propia continúa disponible.


La cobertura de suscripción comprueba consulta propia tras vencimiento, rechazo de activación genérica, concesión con razón y plan permitidos, auditoría de actor real y navegación a estado de suscripción. La regresión de handoff exige organización propia y claims de soporte conservadas; revocar al emisor entre emisión e intercambio debe rechazar sin consumir. Pruebas focales: test_saas_support_handoff.py, test_saas_subscription.mjs y escenarios superadmin; PostgreSQL y reversibilidad de 0072 se verifican por separado.

QA visual local del bundle: login de cuenta sintética, Admin a POS sobre 127.0.0.1:8781, nombre Tacos QA navegador y categorías propias; administración POS presenta siete tarjetas incluyendo comentarios/extras. No se hizo venta ni pedido productivo. Dashboard aún mostraba ERP, detectado para corrección adicional.

`test:saas-remediation` integra las cuatro suites semánticas nuevas en `test:frontend-semantic`, gate que CI ya ejecuta. No se considera que esos casos corrían en CI antes de añadir el script.

### Evidencia dirigida: administracion de sucursal y catalogo

`apps/api/tests/test_saas_branch_catalog_scope.py` usa dos tenants, dos sucursales de A y un supervisor limitado a una sucursal. Comprueba lectura de personal/catalogo propio, ausencia de producto B, rechazo de override B y de otra sucursal A, y que un producto con `catalog_scope=branch` solo admite override desde `source_branch_id`, sin filas cruzadas.

El contrato histórico frontend PCO-007 conserva pruebas internas de recetas, idempotencia y conflictos. Su aserción de montaje comercial se sustituye por el estado explícito fuera de alcance: SaaS no monta RecipesWorkspace. El gate completo detectó esta incompatibilidad y no se eliminó ni omitió la suite.

Categorías: RED actualización cruzada A/B devolvía 200; segunda refutación confirmó lectura de productos con trial vencido 200 sin sucursal. Se verifica pertenencia antes de editar y se autoriza el scope del actor para ambas lecturas, con y sin branch_id. La regresión preserva nombre/estado de A y permite operación propia activa.

Dashboard multitenant: venta y pago reales locales en A, consulta agregada sin sucursal desde B. RED exponía la venta de A; se requiere aislamiento de snapshots, productos, notificaciones de caja y categorías. El ámbito organización es obligatorio o deriva de una sucursal previamente autorizada.

Paridad fiscal receipt/cancel: cada recurso mantiene un comando durable y claim exclusivo antes del PAC; `unknown` bloquea reemision y exige reconciliacion explicita. Cancel conserva hash de motivo/sustitucion para detectar conflicto; ninguna respuesta timeout declara estado fiscal final.

Regresión A07 adicional: `test_partner_config_is_saved_as_redacted_pending_tenant_draft`
parametriza DiDi/Rappi; verifica conservación de secretos vacíos y rechazo 409 del simulador
sin incremento de pedidos. La prueba de configuración DiDi conserva CRUD de mapeos y ahora
espera borrador deshabilitado, rechazo de simulación y bitácora vacía.

`test_saas_simulation_scope.py` verifica por separado Uber, DiDi y Rappi con dos tenants:
pedido propio exitoso y pertenencia persistida, tienda ajena y sucursal ajena rechazadas,
conteos de pedidos y bitácoras inalterados. `test_partner_config_is_saved_as_redacted_pending_tenant_draft`
comprueba además flags falsos al guardar sin secretos por primera vez.

Regresión adicional del simulador: borrar mapeo propio, enviar cantidad inválida sin tienda
y verificar 409 y cero mapeos; impide el commit anticipado detectado en revisión independiente.

`test_saas_waste_reason_scope.py` verifica alta y pertenencia de dos motivos con igual código
en tenants distintos, listado propio, rechazo de edición cruzada sin mutación y edición
permitida. `test_real_waste_draft_confirmation_costing_idempotency_and_reversal` conserva
la regresión del flujo histórico al cambiar el alcance del catálogo de motivos.

La regresión histórica de merma real prueba alta, confirmación idempotente, costo, movimiento,
reversión compensatoria y listado después de exigir organización explícita. La revisión R3 debe
refutar además IDs cruzados A/B para confirmar y revertir antes del cierre productivo.

`test_calculate_suggested_purchases_groups_by_supplier` conserva el nombre histórico pero su
oráculo vigente exige lista vacía incluso con compra anterior: demuestra que costo previo no se
convierte en demanda ficticia.


`test_saas_invoicing_audit.py` cubre commit fallido, bloqueo de reintento, consulta de recurso conocido y replay confirmado para receipt y cancel; también cubre timeout/estado no confirmatorio que preserva `unknown`. `test_fiscal_issue_postgres.py` fuerza dos sesiones concurrentes sobre el mismo receipt y cancel y exige un único resource claim, además de downgrade/reupgrade con preservación de claims y snapshots.

`test_saas_feedback_scope.py` crea comentarios públicos en A/B, verifica listados propios y
rechazo de branch ajena. `test_offline_grant_rejects_organization_different_from_persisted_actor`
prueba la frontera del grant y `test_pco008_cash_sync.py` conserva el recorrido criptográfico.
`test_saas_recipe_scope.py` exige 409 para costeo AI; las pruebas puras del parser se conservan
sin exponer el endpoint en POS-SaaS.

`test_saas_inventory_read_scope.py` crea dos tenants con igual unidad/SKU, verifica listados de
unidades, insumos, stock y almacenes, kardex ajeno vacío, actualización cruzada rechazada y
referencia de unidad ajena rechazada. RED: B no podía crear la misma unidad porque el alta usaba
la organización piloto; luego las actualizaciones cruzadas respondían 200 aunque no mutaban.

`test_saas_role_warehouse_scope.py` crea A/B y reproduce que A podía renombrar un rol ordinario de
B. Verifica por HTTP y servicio que update/delete/permisos rechacen IDs ajenos, que el grant owner
mantenga alcance/permisos/existencia, que create/update de almacén derive la organización del actor,
que una referencia de sucursal ajena falle y que permanezca un solo almacén por sucursal. La prueba
de servicio se ejecuta en SQLite y PostgreSQL; las históricas conservan el rename permitido del rol
owner y la desactivación del almacén sólo después de inactivar la sucursal.

Las regresiones de `test_saas_inventory_read_scope.py`,
`test_saas_branch_catalog_scope.py` y `test_platform_api.py` cubren proyecciones A/B de datos de
plataforma, derivación por sucursal activa, rechazo de ámbito ausente y `bootstrap-status` acotado
al actor. Ruff y `git diff --check` acompañan las pruebas de rutas.


`test_saas_self_invoicing.py` crea dos tenants con el mismo folio y verifica lookup/emisión bajo la clave de A, clave inválida sin PII y que el emisor falso recibe solamente organización, sucursal y pedido de A. `test_saas_fiscal_independent_review.py` refuta receipt con estado `expired` y replay de pedido confirmado con receptor distinto; ambos deben conservar la barrera sin confirmación o sin segunda llamada al proveedor.


`test_legacy_import.py::test_legacy_import_uses_actor_tenant_for_batches_and_same_catalog_codes` crea A/B, importa los mismos SKU y verifica batches, unidades/insumos, categorías/productos y precios separados; también prueba batch y sucursal cruzados sin mutación.

`test_category_selection_groups_values_and_assignments_reject_foreign_tenant` crea A/B,
configura categoría, selector, opción y asociación de A, y refuta lectura de cobertura, creación de
valor y asociación desde B. También verifica que la cobertura propia conserva sólo el producto y
opción de A. `test_saas_modifier_scope.py` conserva el rechazo de lecturas y mutaciones cruzadas
de grupos y opciones de modificadores.

`test_admin_ai_conversation_context_and_review_are_tenant_scoped` crea A/B, captura el contexto del
proveedor falso para ambos, refuta lectura/revisión/continuación cruzadas y verifica que B conserva su
propuesta propia.


`test_saas_procurement_scope.py` crea dos tenants con el mismo código de proveedor, presentación, folio e idempotency key. Comprueba listados y detalle propios, rechazo de proveedor, término, insumo, compra y cancelación ajenos sin mutación, y confirma que los movimientos conservan la organización. La prueba usa Alembic hasta head (0080; unicidad de compra en 0079) para cubrir la unicidad de confirmación por tenant; `test_cash_ledger_postgres.py` conserva la carrera PostgreSQL entre cierre y confirmación de compra.


`test_saas_lean_erp_boundary.py` llama rutas históricas de receta/costeo, lote de producción, transferencia y conteo físico con un tenant válido. Cada familia debe responder `409 feature_out_of_saas_scope` antes de consultar o mutar datos de dominio; merma, almacén único e inventario simple no forman parte de esa prohibición.

## Evidencia histórica del árbol Sol, anterior a esta recuperación

Resultados registrados por el trabajo previo y conservados como antecedente. No corresponden al SHA de esta rama ni se usan como aprobación actual; la evidencia de integración está en `plan-recuperacion-2026-09-07.md` y en el CI del PR. Ejecución histórica con Python 3.14 y PostgreSQL 16 local en
`127.0.0.1:55432/saas_remediation_qa`:

| Gate | Resultado |
|---|---|
| `python -m pytest apps/api/tests -q` | **735 passed, 50 skipped, 0 failed**, 2 warnings de deprecación externa; 10:31:31 |
| `python -m pytest tests/architecture -q` | **148 passed**; 1:04 |
| `python -m ruff check apps/api/restaurant_os apps/api/tests` | **All checks passed** |
| `pnpm typecheck` | **7 paquetes verdes** |
| `pnpm test:frontend-semantic` | **verde**, incluida la suite SaaS de 16 casos |
| builds `admin-web`, `mobile-web`, `pos-web` | **verdes**; Admin/POS conservan avisos no bloqueantes por tamaño de bundle |
| `git diff --check` | **sin errores** |

Los 50 skips corresponden a capacidades/variantes condicionadas declaradas por la suite; las
regresiones R3 nuevas de tenant, locks, idempotencia y migraciones PostgreSQL sí se ejecutaron.
El primer pase completo expuso 26 fallas y 5 errores; el segundo dejó 2 fallas de aislamiento de
settings y 5 fixtures no registradas. El pase final se ejecutó completo después de corregir ambos
problemas, sin relajar las guardas de producción ni agregar silenciamientos.

Esta evidencia histórica no certifica el working tree de recuperación ni la imagen publicada, la configuración
real de EasyPanel, una migración productiva, restauración de backup, proveedor fiscal real ni canary.
